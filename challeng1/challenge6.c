#include <stdio.h>

int main() {
    // Write C code here
  float a,b;
  printf("entrer les nomnr a: ");
  scanf("%f",&a);
  printf("entre le nbr b :");
  scanf("%f",&b);


  printf("a + b  = %.f\n", a+b );
  printf("a * b  = %.f\n", a*b );
  printf("a - b  = %.f\n", a-b );
  printf("a / b  = %.f", a/b );




    return 0;
}
